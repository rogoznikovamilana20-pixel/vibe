# Vibe — CODE REVIEW (чеклист ревью кода)

Применяется к каждой фиче/правке перед merge. Всё ниже — обязательные проверки.

## 1. Компиляция и статика
- [ ] `flutter analyze` — 0 issues
- [ ] `flutter test` (если затронута логика) — всё зелёное
- [ ] Приложение собрано и установлено на эмулятор (`flutter run -d emulator-5554`)

## 2. Безопасность
- [ ] Никаких секретов/ключей/URL в коде — только через `--dart-define` (`EnvConfig`)
- [ ] Никаких прямых записей в таблицы через anon-клиент (всё через RPC/edge, проверка на сервере)
- [ ] Нет `log`/`debugPrint` чувствительных данных (пароли, токены, полные сообщения)
- [ ] Пользовательские вводы не интерполируются в SQL/URL без экранирования
- [x] Серверные сессии и RPC (план 1.1/1.4): `_randomUuid()` убран, `auth_register`/`auth_login`/`auth_logout`
- [x] План 1.2 (JWT + RLS): клиент на `supabase.auth`, RPC-сессии удалены; политики per-row (владелец профиля, членство в чате); столбцовые SELECT-гранты на `profiles`; select-списки колонок явные во всех запросах (SELECT * → 42501 под column grant)

## 3. Производительность
- [ ] Нет `Timer.periodic` в экранах (кроме heartbeat-сервисов) — обновление только по событиям
- [ ] Нет `setState` на каждый скролл / каждую секунду (таймеры записи — изолированные виджеты)
- [ ] Нет полной пересборки списков при точечном изменении (`removeAt+insert` → дельта по id)
- [ ] `Image.network` — всегда с `cacheWidth` и из кэш-слоя, не сырым URL
- [ ] Списки: виртуализация (`ListView.builder`), `const` где возможно
- [ ] Нет чтения медиа целиком в память (`readAsBytes`) для файлов > ~10 МБ
- [ ] Realtime: один канал, без дублей подписок при перестроении

## 4. UX/UI
- [ ] Светлая и тёмная темы — контраст проверить пиксельно/скриншотом на эмуляторе
- [ ] Нет конфликта состояний (дублей DND/архива и т.п.) — единый обработчик
- [ ] Пустые состояния имеют CTA; нет «мёртвых» кнопок (заглушки помечены и не показываются как рабочие)
- [ ] Локализация: все строки через ключи, никаких «кривых» ключей в чужих секциях
- [ ] Анимации по DESIGN_SYSTEM (fade 200, slide 300 overshoot, scale 250)

## 5. Данные и состояние
- [ ] Настройки/архив/скрытые/DND — персистентны (переживают рестарт) либо осознанно оффлайн
- [ ] Реакции/закреп/прочитано — честные (не локальные-имитации), если заявлены
- [ ] Оффлайн-кеш обновляется атомарно, нет гонок при параллельных записях

## 6. Документация
- [ ] TASKS.md (и MASTER_PLAN.md при необходимости) обновлены: пункт → статус
- [ ] Критерий 100% пункта выполнен (билд + проверка на устройстве), иначе пункт не закрывать